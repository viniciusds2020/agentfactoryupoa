fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.12
pandas>=2.2
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27

# LLM
groq>=0.9                # provider principal (rápido, gratuito na tier free)
anthropic>=0.25          # provider alternativo

# Embeddings — dois backends:
#   fastembed (ONNX)        : modelos multilingual leves
#   sentence-transformers   : modelos PT-BR especializados (requer Long Paths)
fastembed>=0.7
sentence-transformers>=3.0   # para rufimelo/Legal-BERTimbau-sts-large e similares

# Vector store
faiss-cpu>=1.8

# Text normalization
nltk>=3.8

# Document parsing (PDF via PyMuPDF, DOCX/PPTX via docling)
pymupdf>=1.24
docling>=2.80
camelot-py>=0.11
tabula-py>=2.9

# Observability
prometheus-client>=0.21

# Dev / tests
pytest>=8
pytest-asyncio>=0.23
pytest-cov>=5
